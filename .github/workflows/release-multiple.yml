name: Auto Release with File Details in Description

on:
  push:
    paths:
      - 'Lunatv/**'  # 'Lunatv/**' 监听整个目录变化   'Lunatv/url.json' 监听目录下单个文件变化
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # === 生成 index.json，并准备 release 描述文本 ===
      - name: Generate index.json and release body
        run: |
          mkdir -p Lunatv
          REPO="mebaddy/TV"
          TAG="latest-files"
          BRANCH="main"
          BASE_URL="https://github.com/${REPO}/releases/download/${TAG}"
          RAW_BASE="https://raw.githubusercontent.com/${REPO}/${BRANCH}"
          BODY_FILE="release_body.txt"

          echo "[" > Lunatv/index.json
          echo "### 📦 文件更新清单（自动生成）" > $BODY_FILE
          echo "" >> $BODY_FILE

          first=true
          for f in $(find Lunatv -type f ! -name "index.json"); do
            if [ "$first" = true ]; then
              first=false
            else
              echo "," >> Lunatv/index.json
            fi
            size=$(stat -c%s "$f")
            mtime=$(date -u -r "$f" +"%Y-%m-%d %H:%M:%S UTC")
            fname=$(basename "$f")
            dlink="${BASE_URL}/${fname}"
            rawlink="${RAW_BASE}/${f}"

            # 生成 JSON 条目
            echo "  {\"name\": \"${fname}\", \"path\": \"${f}\", \"size\": ${size}, \"updated_at\": \"${mtime}\", \"download_url\": \"${dlink}\", \"raw_url\": \"${rawlink}\"}" >> Lunatv/index.json

            # 写入 release 描述
            echo "- **${fname}**" >> $BODY_FILE
            echo "  - 大小：${size} 字节" >> $BODY_FILE
            echo "  - 更新时间：${mtime}" >> $BODY_FILE
            echo "  - 📥 [Release 下载链接](${dlink})" >> $BODY_FILE
            echo "  - 🌐 [Raw 在线访问](${rawlink})" >> $BODY_FILE
            echo "" >> $BODY_FILE
          done
          echo "]" >> Lunatv/index.json

          echo "" >> $BODY_FILE
          echo "🧾 自动生成的索引文件：[index.json](${BASE_URL}/index.json)" >> $BODY_FILE

      - name: Show generated index.json
        run: cat Lunatv/index.json

      - name: Show release body preview
        run: cat release_body.txt

      # === 提交 index.json 到仓库，保持 Raw 可访问 ===
      - name: Commit index.json to repository
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Lunatv/index.json
          if git diff --cached --quiet; then
            echo "No changes in index.json, skipping commit."
          else
            git commit -m "Auto update index.json [skip ci]"
            git push origin main
          fi

      # === 发布 Release，说明中包含详细文件信息 ===
      - name: Publish release with details
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest-files
          name: "Latest Files Auto Release"
          body_path: release_body.txt
          files: Lunatv/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
